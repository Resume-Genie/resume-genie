import React from 'react';

const LiveResume = () => {
  return (
    <section className="h-full ml-10 border border-[var(--med)] w-full">
      LiveResume
    </section>
  );
};

export default LiveResume;
